import React from "react";

interface PaywallModalProps {
  visible: boolean;
  onClose: () => void;
}

export default function PaywallModal({ visible: _visible, onClose: _onClose }: PaywallModalProps) {
  return null;
}
